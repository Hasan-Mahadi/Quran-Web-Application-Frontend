/** @type {import('next').NextConfig} */
const nextConfig = {
  // Comment out or remove output: 'export'
  // output: 'export',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig